// Breadcrumb template
import React from 'react';

// STYLE
import './style.scss';

const Breadcrumb = props => {
    const {} = props;
    
    return (
        <div className="Breadcrumb">Template component</div>
    );
};

export default Breadcrumb;